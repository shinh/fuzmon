[filter]
ignore_process_name = ["^bash$", "^sshd$"]
target_user = "myname"

[output]
format = "json"              # or "msgpack"
path = "/var/log/fuzmon/"
rotate = true
compress = true

[monitor]
interval_sec = 60
cpu_time_percent_threshold = 1.0
stacktrace_percent_threshold = 1.0
